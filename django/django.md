# Django setup

```bash
python -m venv venv

source venv/bin/activate

pip install Django django-extensions django-filter djangorestframework djangorestframework-jsonapi inflection python-dotenv pytz sqlparse tzdata

django-admin startproject mysite djangotutorial
cd djangotutorial
python manage.py runserver
```
